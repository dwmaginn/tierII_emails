# TierII Email System Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env files to version control - they contain sensitive information

# =============================================================================
# CORE EMAIL CONFIGURATION (REQUIRED)
# =============================================================================

# Email address used as the sender for all outgoing emails
# Format: valid email address
# Example: noreply@yourcompany.com
TIERII_SENDER_EMAIL=sender@example.com

# SMTP server hostname or IP address for sending emails
# Format: hostname or IP address
# Example: smtp.office365.com (for Office 365)
TIERII_SMTP_SERVER=smtp.example.com

# SMTP server port number
# Format: integer between 1-65535
# Common values: 587 (TLS), 465 (SSL), 25 (plain)
# Default: 587
TIERII_SMTP_PORT=587

# =============================================================================
# OAUTH 2.0 AUTHENTICATION (REQUIRED)
# =============================================================================

# Azure AD Tenant ID for OAuth authentication
# Format: UUID (e.g., 12345678-1234-1234-1234-123456789012)
# Find in: Azure Portal > Azure Active Directory > Properties
TIERII_TENANT_ID=your-tenant-id-here

# Azure AD Application (Client) ID
# Format: UUID (e.g., 87654321-4321-4321-4321-210987654321)
# Find in: Azure Portal > App Registrations > Your App > Overview
TIERII_CLIENT_ID=your-client-id-here

# Azure AD Application Client Secret
# Format: string (generated secret value)
# Find in: Azure Portal > App Registrations > Your App > Certificates & secrets
# WARNING: Keep this secret secure and never commit to version control
TIERII_CLIENT_SECRET=your-client-secret-here

# =============================================================================
# EMAIL CONTENT CONFIGURATION (REQUIRED)
# =============================================================================

# Subject line for all outgoing emails
# Format: string (supports personalization variables)
# Example: "Important Update from {sender_name}"
TIERII_EMAIL_SUBJECT=Your Email Subject Here

# =============================================================================
# OPTIONAL CONFIGURATION (WITH DEFAULTS)
# =============================================================================

# Display name shown as the sender in email clients
# Format: string
# Default: Derived from TIERII_SENDER_EMAIL (part before @, capitalized)
# Example: "TierII Support Team"
# TIERII_SMTP_SENDER_NAME=Your Sender Name

# Path to external email template file (HTML)
# Format: file path (relative to project root)
# Default: Uses inline template if not specified
# Example: templates/email_template.html
# TIERII_EMAIL_TEMPLATE_PATH=templates/email_template.html

# Number of emails to send in each batch
# Format: integer between 1-1000
# Default: 50
# Validation: Must be positive, warning if > 1000
# TIERII_CAMPAIGN_BATCH_SIZE=50

# Delay in minutes between sending batches
# Format: integer between 0-60
# Default: 5
# Validation: Cannot be negative, warning if > 60
# TIERII_CAMPAIGN_DELAY_MINUTES=5

# =============================================================================
# TEST CONFIGURATION (REQUIRED FOR TEST MODE)
# =============================================================================

# Email address to receive test emails (required when running in test mode)
# Format: valid email address
# Example: test@yourcompany.com
# TIERII_TEST_RECIPIENT_EMAIL=test@example.com

# Fallback first name for personalization in tests
# Format: string
# Default: "Friend"
# TIERII_TEST_FALLBACK_FIRST_NAME=Friend

# Path to CSV file containing contact data for testing
# Format: file path (relative to project root)
# Default: data/contacts/tier_i_tier_ii_emails_verified.csv
# TIERII_TEST_CSV_FILENAME=data/contacts/tier_i_tier_ii_emails_verified.csv

# =============================================================================
# VALIDATION NOTES
# =============================================================================
# 
# Required Variables (will cause startup failure if missing):
# - TIERII_SENDER_EMAIL
# - TIERII_SMTP_SERVER
# - TIERII_TENANT_ID
# - TIERII_CLIENT_ID
# - TIERII_CLIENT_SECRET
# - TIERII_EMAIL_SUBJECT
# - TIERII_TEST_RECIPIENT_EMAIL (only in test mode)
#
# Optional Variables (have defaults or computed values):
# - TIERII_SMTP_PORT (default: 587)
# - TIERII_SMTP_SENDER_NAME (default: derived from sender email)
# - TIERII_EMAIL_TEMPLATE_PATH (default: inline template)
# - TIERII_CAMPAIGN_BATCH_SIZE (default: 50)
# - TIERII_CAMPAIGN_DELAY_MINUTES (default: 5)
# - TIERII_TEST_FALLBACK_FIRST_NAME (default: "Friend")
# - TIERII_TEST_CSV_FILENAME (default: data/contacts/tier_i_tier_ii_emails_verified.csv)
#
# Security Notes:
# - Never commit .env files to version control
# - Keep client secrets secure and rotate them regularly
# - Use environment-specific values for different deployments
# - Consider using Azure Key Vault for production secrets